# TODO: need to work on this element
class EventRegistrarItem:
    def __init__(self, handler, start, interval, repeat):
        self.handler = handler
        self.start = start
        self.interval = interval
        self.repeat = repeat
